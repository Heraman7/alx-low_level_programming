#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0
 */

int main(void)
{
	char c;
	char d;

	c = '0';
	d = 'a';
	while
		(c < 10) {
			putchar(c);
			c++;
		}
	while
		(d <= 'f') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
